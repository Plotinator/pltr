import { SafeAreaView } from 'react-native'
import React from 'react'
import { Text } from 'native-base'

export default function SceneCardModal (props) {
  return <SafeAreaView>
    <Text>Modal</Text>
  </SafeAreaView>
}